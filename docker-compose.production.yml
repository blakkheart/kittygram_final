version: '3'

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    env_file:
      - .env
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    image: blakkheart/kittygram_backend
    env_file: .env
    depends_on:
      - db
    volumes:
      - media:/app/media
      - static:/backend_static
  frontend:
    env_file: .env
    image: blakkheart/kittygram_frontend
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static
  gateway:
    image: blakkheart/kittygram_gateway
    env_file: .env
    ports:
      - 9000:80
    volumes:
      - static:/static
      - media:/media
